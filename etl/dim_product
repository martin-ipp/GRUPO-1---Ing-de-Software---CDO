#df = importar archivo desde load.py

dim_product = (
    df[['Product ID', 'Product Category']]
    .drop_duplicates()
    .reset_index(drop=True)
)

#Crea columna "Grupo de Producto" como los primeros 4 caracteres
dim_product['Product_Group'] = dim_product['Product ID'].astype(str).str[:4]
